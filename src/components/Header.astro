---
---

<header class="header p-12 text-white grid grid-cols-[20%_80%] z-[600] md:grid-cols-[20%_70%_10%] items-center">
    <div>
        <img src="/images/logo-no.png" class="max-w-[60px]"/>
    </div>
    <div class="md:block text-right pr-12 flex items-center justify-center">
        <p>socials</p>
    </div>
    <div class="md:hidden text-right items-center justify-center justify-items-center">
        movile nav
    </div>
    <div class="hidden md:flex justify-end justify-items-right">
        <div class="nav-btn-wrapper px-4 py-8">
            <p class="nav-btn-item"></p>
        </div>
    </div>
</header>

<style lang="scss">

    .nav-btn-wrapper {
        border-radius: 5px;
        transition: all 0.3s ease;
        width: 65px;
        position: relative;

        &::before {
            content:"MENU";
            position:absolute;
            top:20px;
            font-size: 12px;
            opacity:0;
            transition: all 0.3s ease; 

        }
        &:hover {
            background:rgba(255,255,255,0.2);
            transition: all 0.3s ease;
            cursor:pointer;

            &::before {
            content:"MENU";
            position:absolute;
            top:23px;
            font-size: 12px;
            opacity:1;
            transition: all 0.3s ease; 
            transition-delay: 0.2s;
        }

            .nav-btn-item {
                    opacity: 0;
                        transition: all .5s ease;
                &::before {
                    margin-top:0; 
                    transition: all .5s ease;
                }
                &::after {
                    margin-top:0; 
                    transition: all .5s ease;
                }
            }
        }

    }
    .nav-btn-item {
        width: 35px;
        height: 2px;
        background: #fff;
      
        transition: all 0.3s ease;

      
        &::before {
        content:"";
        position: absolute;
        width: 35px;
        height: 2px;
        background: #fff;
        margin-top: 7px;
        transition: all .5s ease;
        }
        &::after {
        content:"";
        position: absolute;
        width: 35px;
        height: 2px;
        background: #fff;
        margin-top: -7px;
        transition: all .5s ease;
        }

    }
</style>